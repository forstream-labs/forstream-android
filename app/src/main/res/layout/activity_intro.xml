<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:background="@android:color/white"
  android:clipToPadding="false"
  android:orientation="vertical"
  android:padding="@dimen/margin_lg">

  <LinearLayout
    android:layout_width="match_parent"
    android:layout_height="0dp"
    android:layout_weight="1">

  </LinearLayout>

  <com.google.android.material.button.MaterialButton
    android:id="@+id/sign_in_with_facebook_button"
    style="@style/Button.Large"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:text="@string/activity_intro_sign_in_with_facebook"
    app:backgroundTint="@color/com_facebook_button_background_color"
    app:rippleColor="@color/com_facebook_button_background_color_pressed"/>

  <com.google.android.material.button.MaterialButton
    android:id="@+id/sign_in_with_google_button"
    style="@style/Button.Large"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:text="@string/activity_intro_sign_in_with_google"
    android:textColor="@color/google_btn_text"
    app:backgroundTint="@android:color/white"
    app:icon="@drawable/ic_google"
    app:iconTint="@null"
    app:rippleColor="@android:color/darker_gray"/>

</LinearLayout>
